# Allow legacy peer deps during CI/build to avoid ERESOLVE failures
# This repository uses a release-candidate mix of @rneui packages
legacy-peer-deps=true

# Ensure devDependencies are installed (eslint/prettier/jest are devDependencies)
# Some environments set `npm config set omit dev` globally.
# npm requires `omit` to be one or more of: dev, optional, peer.
# Setting it to `optional` overrides any global `omit=dev` and keeps devDependencies installed.
omit=optional
