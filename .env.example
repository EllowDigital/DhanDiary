## Example environment variables (DO NOT commit real secrets)
# Copy to .env locally and populate with real values.

# Neon Postgres connection string
NEON_URL=postgresql://<user>:<pass>@<host>/<db>?sslmode=require

# Vexo analytics key (rotate if leaked)
VEXO_API_KEY=your-vexo-api-key-here
NEON_URL=YOUR_NEON_URL
