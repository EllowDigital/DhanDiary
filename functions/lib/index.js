// Cloud Functions removed — neutral stub to avoid parsing/lint/runtime errors.
// Keep a valid CommonJS export so tooling and builds that import this file
// won't fail. The real functions were intentionally removed for local-only mode.

module.exports = {
  onEntryWrite: () => {
    // disabled
  },
  backfillSummaries: () => {
    // disabled
  },
};
