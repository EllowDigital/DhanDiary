#!/usr/bin/env node
// Firestore audit script removed for local-only mode.
// This project has been migrated to offline-first storage; any Firestore
// audit should be performed outside of this repository against your
// remote Firestore instance if needed.

console.log('Audit script disabled: project is local-only.');
process.exit(0);
